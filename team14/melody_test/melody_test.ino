int reles[] = {2,3,4,5};

void setup() {
  // put your setup code here, to run once:
  for(int i = 0; i < 4; i++)
    pinMode(reles[i], OUTPUT);  
}

void loop() {
  // put your main code here, to run repeatedly:
  for(int i = 0; i < 4; i++)
  {
    long lastTime = clock();
  }
}
